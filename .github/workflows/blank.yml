name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello Step
        run:  |
        echo 'This is the line one'
        echo 'This is the line two'
